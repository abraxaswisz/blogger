<div>
<h4>Comment by:
<%= comment.author_name %>
</h4>
<p class="comment">
<%= comment.body %>
</p>
<p>
	 
	<p>
		Posted <%= distance_of_time_in_words(Time.now, comment.created_at) %> ago
	</p>
	<p>Time after publication of article <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %>  later
	</p>

</p>
<p class="destroy">
<%= link_to "delete", article_comment_path(@article, comment), data: {confirm: "Are you sure?"}, method: :delete %>
</p>
</div>