<h1>All tags</h1>
<ul>
<% @tags.each do |tag| %>
	<li>
		<%= link_to tag.name, tag_path(tag) %>
	</li>
	<%= link_to "delete", tag_path(tag), data: {confirm: "Are you sure?"}, method: :delete %>
<% end %>


</ul>